<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ERPProject">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Worker.Activities.WorkerLoginActivity" />
        <activity android:name=".Admin.Activities.AdminLoginActivity" />
        <activity android:name=".Worker.Activities.WorkerDashboardActivity" />
        <activity android:name=".Admin.Activities.AdminDashboardActivity" />
        <activity android:name=".Admin.Activities.AdminWorkerActivity"/>
        <activity android:name=".Admin.Activities.AdminLoanActivity"/>
        <activity android:name=".Admin.Activities.AdminVacationActivity"/>
        <activity android:name=".Admin.Activities.AdminAnnouncementActivity"/>
        <activity android:name=".Admin.Activities.AdminAttendanceActivity"/>



        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>